sync:
  app:
    mode: "two-way-resolved"
    alpha: "./"
    beta: "docker-on-vagrant:/home/vagrant/app"
    ignore:
      vcs: true
      paths:
        - "/node_modules"
        - "/log"
        - "/tmp"
